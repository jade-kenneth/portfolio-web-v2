import { Box } from "@chakra-ui/react";
import React from "react";

const Hero = () => {
  return (
    <Box maxHeight="80px" background={"primary"} color={"red"}>
      I am hero
    </Box>
  );
};

export default Hero;
